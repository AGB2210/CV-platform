[pytest]
# Run from backend/ so `from app...` and `from tests...` both resolve.
testpaths = tests
addopts = -q
filterwarnings =
    # Starlette nags that its TestClient uses httpx; that's expected and not
    # something this project controls.
    ignore::DeprecationWarning
