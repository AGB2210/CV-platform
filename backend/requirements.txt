# Core backend dependencies (Phase 0+).
#
# Deliberately does NOT include torch / ultralytics / transformers. Those pull
# down several GB and are only needed from Phase 2 onward — see
# requirements-ml.txt. Keeping them apart means this skeleton installs in
# seconds, and a later CPU-only or CUDA-specific torch build can be chosen
# without touching this file.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Schema migrations. init_db() runs `upgrade head` at startup; the baseline
# revision converges any pre-1.0 database (see backend/migrations/).
alembic>=1.13.0

# Required by FastAPI to parse multipart/form-data — i.e. file uploads (Phase 1).
python-multipart>=0.0.12

# Image metadata (width/height) on upload, and drawing predictions later.
pillow>=11.0.0

# Reading data.yaml when importing a YOLO dataset. Declared explicitly even
# though uvicorn[standard] happens to pull it in: depending on a transitive
# dependency for something we import directly means a change in uvicorn's extras
# silently breaks dataset import. Declare what you import.
pyyaml>=6.0
